## 1.0.0

* `blocEffectsTest` — adds effect assertions to `blocTest` from `package:bloc_test`.
* `skipEffects` — skip leading effects (mirrors `skip` for states).
* Delegates all test infrastructure to `blocTest` for maintainability and consistency.
